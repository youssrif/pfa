/* import React,{useState} from 'react'

function CalenderIteam(props) {
    const [month, setMonth] = useState(null);
    let num = parseInt(props.value)
    return (
        <li style={month === num ? { backgroundColor: 'rgb(161, 170, 248)', transition: 'all ease' } : {}} value={props.value}  onClick={(e) => setMonth(e.target.value)} id = {props.id} htmlFor={props.id}>{props.name}</li>
    )
}

export default CalenderIteam


 */
